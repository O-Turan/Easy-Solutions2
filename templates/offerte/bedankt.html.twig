{% extends 'base.html.twig' %}

{% block title %}Bedankt voor uw aanvraag - Easy Solutions{% endblock %}

{% block meta_description %}
    Bedankt voor uw aanvraag bij Easy Solutions. Wij nemen zo spoedig mogelijk contact met u op voor een passende telecom- of VoIP-oplossing.
{% endblock %}

{% block meta_robots %}
    <meta name="robots" content="noindex, follow">
{% endblock %}

{% block og_title %}Bedankt voor uw aanvraag - Easy Solutions{% endblock %}
{% block og_description %}
    Uw aanvraag is succesvol ontvangen. Wij nemen spoedig contact op.
{% endblock %}

{% block body %}

    <section class="d-flex align-items-center justify-content-center" style="min-height:70vh; background:#f4f6f9;">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-7 col-md-9">

                    <div class="thankyou-card text-center p-5 rounded-4 shadow-sm bg-white">

                        <div class="icon-wrapper mb-4">
                            ✅
                        </div>

                        <h1 class="fw-bold mb-3">Bedankt voor uw aanvraag!</h1>

                        <p class="lead text-muted mb-4">
                            Wij nemen zo snel mogelijk contact met u op.
                        </p>

                        <a href="{{ path('app_home') }}" class="btn btn-primary btn-lg rounded-pill px-5">
                            Terug naar homepagina
                        </a>

                    </div>

                </div>
            </div>
        </div>
    </section>

    <style>
        /* Card styling */
        .thankyou-card {
            animation: fadeUp 0.6s ease forwards;
        }

        /* Check icon */
        .icon-wrapper {
            width: 64px;
            height: 64px;
            margin: 0 auto;
            border-radius: 50%;
            background: rgba(0, 123, 255, 0.1);
            color: #0d6efd;
            font-size: 28px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Button override */
        .thankyou-card .btn-primary {
            background: linear-gradient(135deg, #001f4d, #b30000);
            border: none;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        .thankyou-card .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }

        /* Animation */
        @keyframes fadeUp {
            from {
                opacity: 0;
                transform: translateY(25px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>

{% endblock %}

{% block json_ld %}
    <script type="application/ld+json">
        {
          "@context": "https://schema.org",
          "@type": "WebPage",
          "name": "Bedankt voor uw aanvraag",
          "description": "Bevestigingspagina na versturen van een offerteaanvraag bij Easy Solutions.",
          "url": "{{ app.request.uri }}",
  "isPartOf": {
    "@type": "WebSite",
    "name": "Easy Solutions",
    "url": "https://www.easysolutions.nl"
  }
}
    </script>
{% endblock %}
