{% extends 'base.html.twig' %}

{% block title %}Offerte aanvragen - Easy Solutions{% endblock %}

{% block meta_description %}
    Vraag vrijblijvend een offerte aan voor VoIP, hosted telefonie, SIP-trunks of zakelijke telecomoplossingen bij Easy Solutions. Binnen 24 uur reactie.
{% endblock %}

{% block meta_robots %}
    <meta name="robots" content="index, follow">
{% endblock %}

{% block body %}

    <!-- HEADER -->
    <header class="py-5 position-relative text-white" style="background: linear-gradient(135deg, #001f4d, #b30000);">
        <div class="container py-5 text-center">
            <h1 class="display-5 fw-bold mb-3">Vraag een offerte aan</h1>
            <p class="lead opacity-75 mb-0">
                Ontvang binnen 24 uur een vrijblijvende offerte voor jouw zakelijke telecomoplossing.
            </p>
        </div>

        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 120"
             class="position-absolute start-0 w-100"
             style="bottom:-1px"
             preserveAspectRatio="none"
             height="60">
            <path fill="#ffffff"
                  d="M0,64L48,74.7C96,85,192,107,288,112C384,117,480,107,576,96C672,85,768,75,864,80C960,85,1056,107,1152,112C1248,117,1344,107,1392,101.3L1440,96L1440,120L0,120Z"/>
        </svg>
    </header>

    <!-- CONTENT -->
    <main class="py-5">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8">

                    <div class="card shadow-sm rounded-4 p-4 p-lg-5">

                        <h2 class="h4 fw-bold text-center mb-2">Offerte aanvragen</h2>
                        <p class="text-center text-muted small mb-4">
                            Offerte voor VoIP, hosted telefonie en zakelijke telecomoplossingen op maat.
                        </p>

                        <!-- INFO BLOKKEN -->
                        <div class="row g-3 mb-4 text-center">
                            <div class="col-md-4">
                                <div class="p-3 bg-light rounded-4 h-100">
                                    <strong>📞 Zakelijke telefonie</strong>
                                    <div class="small text-muted">VoIP, Hosted PBX & SIP-trunks</div>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="p-3 bg-light rounded-4 h-100">
                                    <strong>⚡ Snel geregeld</strong>
                                    <div class="small text-muted">Binnen 24 uur reactie</div>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="p-3 bg-light rounded-4 h-100">
                                    <strong>🤝 Persoonlijk advies</strong>
                                    <div class="small text-muted">Geen standaard offertes</div>
                                </div>
                            </div>
                        </div>

                        {% if errors is defined and errors %}
                            <div class="alert alert-danger">
                                <ul class="mb-0">
                                    {% for error in errors %}
                                        <li>{{ error }}</li>
                                    {% endfor %}
                                </ul>
                            </div>
                        {% endif %}

                        <!-- FORMULIER -->
                        <form action="{{ path('offerte_send') }}" method="POST">
                            <input type="text" name="_honey" style="display:none">

                            <div class="mb-3">
                                <label class="form-label">Naam</label>
                                <input type="text" name="Naam" class="form-control" required value="{{ formData.Naam ?? '' }}">
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Bedrijf</label>
                                <input type="text" name="Bedrijf" class="form-control" required value="{{ formData.Bedrijf ?? '' }}">
                            </div>

                            <div class="mb-3">
                                <label class="form-label">E-mail</label>
                                <input type="email" name="email" class="form-control" required value="{{ formData.email ?? '' }}">
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Telefoon</label>
                                <input type="text" name="Telefoonnummer" class="form-control" required value="{{ formData.Telefoonnummer ?? '' }}">
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Aantal accounts</label>
                                <input type="number" name="Aantal_accounts" min="1" max="50" class="form-control"
                                       value="{{ formData.Aantal_accounts ?? 1 }}" required>
                            </div>

                            <div class="mb-4">
                                <label class="form-label">Bericht</label>
                                <textarea name="Bericht" rows="4" class="form-control">{{ formData.Bericht ?? '' }}</textarea>
                            </div>

                            <!-- BUTTON -->
                            <div class="text-center">
                                <button type="submit"
                                        class="btn btn-super px-5 py-3 rounded-pill shadow-lg position-relative overflow-hidden">
                                    <span class="btn-text">Verstuur aanvraag 💬</span>
                                    <span class="btn-glow"></span>
                                </button>

                                <div class="mt-4 d-flex justify-content-center gap-4 flex-wrap text-muted small">
                                    <div>✅ Binnen 24 uur reactie</div>
                                    <div>🔒 Gegevens veilig verwerkt</div>
                                    <div>📞 Persoonlijk contact</div>
                                </div>

                                <p class="form-text mt-3 text-dark small">
                                    Door te verzenden ga je akkoord met onze
                                    <a href="{{ path('app_privacy_beleid') }}" class="text-decoration-none">
                                        privacy voorwaarden
                                    </a>.
                                </p>
                            </div>
                        </form>

                    </div>

                </div>
            </div>
        </div>
    </main>

    <!-- BUTTON CSS -->
    <style>
        .btn-super {
            background: linear-gradient(135deg, #001f4d, #b30000);
            color: #fff;
            font-weight: 700;
            font-size: 1.05rem;
            letter-spacing: .5px;
            transition: transform .3s ease, box-shadow .3s ease;
            position: relative;
        }
        .btn-super .btn-text {
            position: relative;
            z-index: 2;
            color: #fff;
        }
        .btn-super .btn-glow {
            position: absolute;
            inset: -50%;
            background: radial-gradient(circle, rgba(255,255,255,.25) 0%, transparent 70%);
            opacity: 0;
            transition: opacity .3s ease, transform .5s ease;
            z-index: 1;
        }
        .btn-super:hover {
            transform: translateY(-4px) scale(1.02);
            box-shadow: 0 12px 35px rgba(0,0,0,.3);
        }
        .btn-super:hover .btn-glow {
            opacity: 1;
            transform: rotate(45deg);
        }
        .btn-super:active {
            transform: scale(.98);
        }
    </style>

{% endblock %}

{% block og_title %}Offerte aanvragen - Easy Solutions{% endblock %}
{% block og_description %}
    Vraag vrijblijvend een offerte aan voor zakelijke telecomdiensten zoals VoIP, hosted PBX en SIP-trunks.
{% endblock %}
