{% extends 'base.html.twig' %}

{% block title %}Offerte aanvragen - Easy Solutions{% endblock %}

{% block meta_description %}
    Vraag vrijblijvend een offerte aan voor VoIP, hosted telefonie en zakelijke telecomoplossingen. Persoonlijk advies binnen 24 uur.
{% endblock %}

{% block body %}

    <!-- reCAPTCHA v3 -->
    <script src="https://www.google.com/recaptcha/api.js?render={{ recaptcha_site_key }}"></script>

    <header class="hero-offerte position-relative text-white">
        <div class="container py-5 text-center">
            <h1 class="display-4 fw-bold mb-3">Vraag een persoonlijke offerte aan</h1>
            <p class="lead opacity-75">Geen standaardpakketten. Wel een oplossing die past bij jouw bedrijf.</p>
        </div>

        <svg viewBox="0 0 1440 120" class="hero-wave" preserveAspectRatio="none">
            <path fill="#fff"
                  d="M0,64L48,74.7C96,85,192,107,288,112C384,117,480,107,576,96C672,85,768,75,864,80C960,85,1056,107,1152,112C1248,117,1344,107,1392,101.3L1440,96L1440,120L0,120Z"/>
        </svg>
    </header>

    <main class="py-5">
        <div class="container">
            <div class="row g-5 justify-content-center align-items-start">

                <!-- FORM -->
                <div class="col-lg-7">
                    <div class="glass-card p-4 p-lg-5">

                        <div class="text-center mb-4">
                            <h2 class="h4 fw-bold">Offerte aanvragen</h2>
                            <p class="text-muted small">Binnen 24 uur persoonlijk contact</p>
                        </div>

                        {% if errors is defined and errors %}
                            <div class="alert alert-danger">
                                <ul class="mb-0">
                                    {% for error in errors %}
                                        <li>{{ error }}</li>
                                    {% endfor %}
                                </ul>
                            </div>
                        {% endif %}

                        <form id="offerteForm" action="{{ path('offerte_send') }}" method="POST">
                            <input type="text" name="_honey" style="display:none">
                            <input type="hidden" name="g-recaptcha-response" id="g-recaptcha-response">

                            <div class="row g-3">

                                <div class="col-md-6">
                                    <label class="form-label">Naam *</label>
                                    <input type="text" name="Naam"
                                           class="form-control form-modern"
                                           placeholder="Uw naam"
                                           required value="{{ formData.Naam|default('') }}">
                                </div>

                                <div class="col-md-6">
                                    <label class="form-label">Bedrijfsnaam *</label>
                                    <input type="text" name="Bedrijf"
                                           class="form-control form-modern"
                                           placeholder="Naam van uw bedrijf"
                                           required value="{{ formData.Bedrijf|default('') }}">
                                </div>

                                <div class="col-md-6">
                                    <label class="form-label">E-mailadres *</label>
                                    <input type="email" name="email"
                                           class="form-control form-modern"
                                           placeholder="naam@bedrijf.nl"
                                           required value="{{ formData.email|default('') }}">
                                </div>

                                <div class="col-md-6">
                                    <label class="form-label">Telefoonnummer</label>
                                    <input type="tel" name="Telefoonnummer"
                                           class="form-control form-modern"
                                           placeholder="06 12345678"
                                           value="{{ formData.Telefoonnummer|default('') }}">
                                </div>

                                <div class="col-md-6">
                                    <label class="form-label">Aantal gebruikers</label>
                                    <input type="number" name="Aantal_accounts"
                                           class="form-control form-modern"
                                           min="1" max="100"
                                           placeholder="Bijv. 5"
                                           value="{{ formData.Aantal_accounts|default(1) }}">
                                </div>

                                <div class="col-12">
                                    <label class="form-label">Toelichting (optioneel)</label>
                                    <textarea name="Bericht" rows="4"
                                              class="form-control form-modern"
                                              placeholder="Uw bericht...">{{ formData.Bericht|default('') }}</textarea>
                                </div>

                                <div class="col-12 text-center mt-4">
                                    <button type="submit"
                                            class="btn btn-hero px-5 py-3 rounded-pill position-relative overflow-hidden">
                                        Offerte aanvragen →
                                        <span class="btn-glow"></span>
                                    </button>
                                    <p class="small text-muted mt-3 mb-0">
                                        Door te verzenden ga je akkoord met onze
                                        <a href="{{ path('app_privacy_beleid') }}" class="text-decoration-none">Privacybeleid</a>.
                                    </p>
                                </div>

                            </div>
                        </form>

                    </div>
                </div>

                <!-- INFO CARDS -->
                <div class="col-lg-4">
                    <div class="info-stack">
                        <div class="info-card">
                            <strong>Waarom Easy Solutions?</strong>
                            <p>Geen pakketten, maar oplossingen op maat.</p>
                        </div>
                        <div class="info-card highlight">
                            <strong><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#d11010" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-zap-icon lucide-zap"><path d="M4 14a1 1 0 0 1-.78-1.63l9.9-10.2a.5.5 0 0 1 .86.46l-1.92 6.02A1 1 0 0 0 13 10h7a1 1 0 0 1 .78 1.63l-9.9 10.2a.5.5 0 0 1-.86-.46l1.92-6.02A1 1 0 0 0 11 14z"/></svg>Snel</strong>
                            <p>Meestal dezelfde werkdag reactie.</p>
                        </div>
                        <div class="info-card">
                            <strong><svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#d11010" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-lock-icon lucide-lock"><rect width="18" height="11" x="3" y="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>Transparant</strong>
                            <p>Heldere prijzen, geen verrassingen.</p>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </main>

    <script>
        document.getElementById('offerteForm').addEventListener('submit', function(e) {
            e.preventDefault();
            grecaptcha.ready(function() {
                grecaptcha.execute('{{ recaptcha_site_key }}', {action: 'offerte'}).then(function(token) {
                    document.getElementById('g-recaptcha-response').value = token;
                    document.getElementById('offerteForm').submit();
                });
            });
        });
    </script>

    <style>
        /* HERO */
        .hero-offerte{background:radial-gradient(circle at top left,#002b6f,#00152f)}
        .hero-wave{position:absolute;bottom:-1px;width:100%;height:60px}

        /* FORM CARD */
        .glass-card{
            background:rgba(255,255,255,.95);
            border-radius:24px;
            box-shadow:0 30px 80px rgba(0,0,0,.12);
        }
        .form-label{font-size:.8rem;font-weight:600;color:#374151;margin-bottom:.25rem}
        .form-modern{border-radius:14px;padding:14px 16px;border:1px solid #e5e7eb}
        .form-modern:focus{border-color:#002b6f;box-shadow:none}

        /* BUTTON */
        .btn-hero{
            background: linear-gradient(135deg,#002b6f,#00152f);
            color:#fff !important;
            font-weight:700;
            font-size:1rem;
            position:relative;
            overflow:hidden;
            transition: transform .3s ease, box-shadow .3s ease, background .3s ease;
        }
        .btn-hero .btn-glow{
            content:"";
            position:absolute;
            top:-50%;
            left:-50%;
            width:200%;
            height:200%;
            background:radial-gradient(circle at center,rgba(255,255,255,.25) 0%,transparent 70%);
            opacity:0;
            transition:opacity .3s, transform .5s;
            z-index:1;
        }
        .btn-hero:hover,
        .btn-hero:focus,
        .btn-hero:active{
            color:#fff !important;
            transform:translateY(-4px) scale(1.02);
            box-shadow:0 12px 35px rgba(0,0,0,.25);
        }
        .btn-hero:hover .btn-glow{
            opacity:1;
            transform:rotate(45deg);
        }

        /* INFO CARDS */
        .info-stack{display:flex;flex-direction:column;gap:1rem}
        .info-card{
            background:#fff;
            border-radius:18px;
            padding:1.25rem;
            box-shadow:0 12px 30px rgba(0,0,0,.08);
            transition: transform .3s ease, box-shadow .3s ease;
        }
        .info-card:hover{transform:translateY(-4px);box-shadow:0 20px 40px rgba(0,0,0,.12);}
        .info-card.highlight{
            background:linear-gradient(135deg,#002b6f,#00152f);
            color:#fff;
        }
    </style>

{% endblock %}
