{% extends 'base.html.twig' %}

{% block title %}Offerte aanvragen - Easy Solutions{% endblock %}

{% block meta_description %}
    Vraag vrijblijvend een offerte aan voor VoIP, hosted telefonie, SIP-trunks of zakelijke telecomoplossingen bij Easy Solutions. Binnen 24 uur reactie.
{% endblock %}

{% block meta_robots %}
    <meta name="robots" content="index, follow">
{% endblock %}

{% block body %}
    <div class="container mt-5" style="max-width:600px;">
        <h1 class="mb-4">Vraag vrijblijvend een offerte aan</h1>

        {% if errors is defined %}
            <div class="alert alert-danger">
                <ul>
                    {% for error in errors %}
                        <li>{{ error }}</li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}

        <form action="{{ path('offerte_send') }}" method="POST">
            <input type="text" name="_honey" style="display:none">

            <div class="mb-3">
                <label class="form-label">Naam</label>
                <input type="text" name="Naam" class="form-control" value="{{ formData.Naam ?? '' }}" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Bedrijf</label>
                <input type="text" name="Bedrijf" class="form-control" value="{{ formData.Bedrijf ?? '' }}" required>
            </div>

            <div class="mb-3">
                <label class="form-label">E-mail</label>
                <input type="email" name="email" class="form-control" value="{{ formData.email ?? '' }}" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Telefoon</label>
                <input type="text" name="Telefoonnummer" class="form-control" value="{{ formData.Telefoonnummer ?? '' }}" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Aantal accounts</label>
                <input type="number" name="Aantal_accounts" class="form-control" min="1" max="50" value="{{ formData.Aantal_accounts ?? 1 }}" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Bericht</label>
                <textarea name="Bericht" class="form-control" rows="4">{{ formData.Bericht ?? '' }}</textarea>
            </div>

            <button type="submit" class="btn btn-primary w-100">Verstuur aanvraag</button>
        </form>
    </div>


{% endblock %}

{% block og_title %}Offerte aanvragen - Easy Solutions{% endblock %}

{% block og_description %}
    Vraag vrijblijvend een offerte aan voor zakelijke telecomdiensten zoals VoIP, hosted PBX, SIP-trunks en meer.
{% endblock %}

{% block json_ld %}
    <script type="application/ld+json">
        {
          "@context": "https://schema.org",
          "@type": "ContactPage",
          "name": "Offerte aanvragen - Easy Solutions",
          "description": "Vraag vrijblijvend een offerte aan voor VoIP, hosted telefonie en zakelijke telecomoplossingen.",
          "url": "{{ app.request.uri }}",
  "publisher": {
    "@type": "Organization",
    "name": "Easy Solutions",
    "url": "https://www.easysolutions.nl",
    "logo": {
      "@type": "ImageObject",
      "url": "{{ asset('assets/easy-solutions-high-resolution-logo-transparent.png') }}"
    }
  },
  "contactPoint": {
    "@type": "ContactPoint",
    "contactType": "Sales",
    "email": "info@easysolutions.nl",
    "telephone": "+31 880666100",
    "areaServed": "NL",
    "availableLanguage": ["Dutch", "English"]
  }
}
    </script>
{% endblock %}
