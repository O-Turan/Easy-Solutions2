{% extends 'base.html.twig' %}

{% block title %}Nummerportering - Easy Solutions{% endblock %}

{% block body %}

    <!-- HERO SECTION -->
    <section class="hero-section d-flex align-items-center justify-content-center text-center text-white">
        <div class="hero-bg"></div>
        <div class="hero-overlay"></div>
        <div class="container position-relative hero-content">
            <h1 class="display-3 fw-bold animate__animated animate__fadeInDown">Nummerportering</h1>
            <p class="lead mb-4 animate__animated animate__fadeInUp animate__delay-1s">
                Soepele en snelle overdracht van uw telefoonnummers zonder onderbrekingen.
            </p>
            <a href="#info-section" class="scroll-down">↓</a>
        </div>
    </section>

    <!-- UITLEG NUMMERPORTERING -->
    <section class="py-5 bg-white" id="info-section">
        <div class="container">
            <h2 class="fw-bold mb-4">Wat is Nummerportering?</h2>
            <p>
                Nummerportering is een proces waarbij je jouw bestaande telefoonnummers neemt naar een nieuwe provider of telefoniesysteem.
                Dit zorgt ervoor dat jouw klanten jou nog steeds kunnen bereiken op dezelfde nummers, ook als jij overstapt.
                Het proces wordt met uiterste zorg uitgevoerd om onderbrekingen te voorkomen, zodat jouw bereikbaarheid altijd gegarandeerd is.
                Met nummerportering behoud je gegevens in jouw telefoniesystemen en hoef je geen nieuwe contactgegevens aan te maken.
            </p>
        </div>
    </section>

    <!-- INFORMATIE SECTION -->
    <section class="py-5 bg-light">
        <div class="container">
            <h2 class="fw-bold text-center mb-4">Waarom kiezen voor onze nummerportering?</h2>
            <p class="text-center mb-5">
                Wij zorgen voor een veilige en snelle overdracht van uw telefoonnummers, zodat uw bedrijf altijd bereikbaar blijft.
            </p>

            <div class="row g-4">
                <div class="col-md-6 col-lg-3">
                    <div class="card info-card h-100 shadow-sm text-center p-4">
                        <div class="icon-circle">⚡</div>
                        <h5 class="mt-3 fw-bold">Snelle Overdracht</h5>
                        <p>Uw nummers worden zonder vertraging overgezet naar het nieuwe systeem.</p>
                    </div>
                </div>
                <div class="col-md-6 col-lg-3">
                    <div class="card info-card h-100 shadow-sm text-center p-4">
                        <div class="icon-circle">🔄</div>
                        <h5 class="mt-3 fw-bold">Geen Onderbrekingen</h5>
                        <p>Uw bereikbaarheid blijft continu, zodat uw klanten u altijd kunnen bereiken.</p>
                    </div>
                </div>
                <div class="col-md-6 col-lg-3">
                    <div class="card info-card h-100 shadow-sm text-center p-4">
                        <div class="icon-circle">👔</div>
                        <h5 class="mt-3 fw-bold">Professionele Afhandeling</h5>
                        <p>Onze specialisten begeleiden het gehele proces.</p>
                    </div>
                </div>
                <div class="col-md-6 col-lg-3">
                    <div class="card info-card h-100 shadow-sm text-center p-4">
                        <div class="icon-circle">📊</div>
                        <h5 class="mt-3 fw-bold">Volledige Transparantie</h5>
                        <p>U ontvangt duidelijke updates en een overzicht van de stappen.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA SECTION -->
    <section class="py-5 text-center text-white cta-section">
        <div class="container">
            <h2 class="fw-bold mb-3">Klaar om uw nummers over te zetten?</h2>
            <p class="lead mb-4">Neem contact op en laat ons uw nummerportering professioneel regelen.</p>
            <a href="/contact" class="btn btn-cta btn-lg">Contact opnemen</a>
        </div>
    </section>

    <!-- STYLES -->
    <style>
        /* HERO */
        .hero-section{
            height: 60vh;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            overflow: hidden;
        }
        .hero-bg{
            position: absolute;
            inset: 0;
            background: url('{{ asset("assets/nummerportering-photo.webp") }}') center/cover no-repeat;
            background-size: cover;
            filter: brightness(0.6);
            transform: scale(1.1);
            z-index: 1;
            transition: transform 1.5s ease;
        }
        .hero-section:hover .hero-bg{
            transform: scale(1.15);
        }
        .hero-overlay{
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(0,31,77,0.7), rgba(179,0,0,0.6));
            z-index: 2;
        }
        .hero-content{
            position: relative;
            z-index: 3;
        }

        /* SCROLL DOWN ARROW */
        .scroll-down{
            display: inline-block;
            margin-top: 1rem;
            font-size: 2rem;
            animation: bounce 2s infinite;
            color: #fff;
        }
        @keyframes bounce{
            0%,20%,50%,80%,100%{transform:translateY(0)}
            40%{transform:translateY(-10px)}
            60%{transform:translateY(-5px)}
        }

        /* INFO CARDS */
        .info-card{
            border-radius: 12px;
            transition: transform 0.45s cubic-bezier(.22,1,.36,1), box-shadow 0.45s;
            background: #fff;
        }
        .info-card:hover{
            transform: translateY(-15px) scale(1.05);
            box-shadow: 0 25px 50px rgba(0,0,0,.2);
        }
        .icon-circle{
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: auto;
            font-size: 1.8rem;
            background: linear-gradient(135deg, #001f4d, #b30000);
            color: #fff;
            transition: transform 0.35s;
        }
        .info-card:hover .icon-circle{
            transform: scale(1.3) rotate(5deg);
        }

        /* CTA */
        .cta-section{
            background: linear-gradient(135deg,#001f4d,#b30000);
        }
        .btn-cta{
            background: linear-gradient(135deg,#e53935,#b71c1c);
            color: #fff;
            border: none;
            padding: 0.75rem 1.8rem;
            font-weight: 600;
            border-radius: 10px;
            transition: all 0.35s;
        }
        .btn-cta:hover{
            background: linear-gradient(135deg,#b71c1c,#e53935);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 15px 30px rgba(183,28,28,.6);
        }

        /* PARALLAX EFFECT */
        @media (prefers-reduced-motion: no-preference) {
            .hero-bg{will-change: transform;}
        }

        /* RESPONSIVE */
        @media (max-width: 768px){
            .hero-section h1{font-size:2.5rem;}
            .hero-section p{font-size:1.1rem;}
            .info-card{padding:2rem 1rem;}
        }
    </style>

    <!-- OPTIONAL: Parallax JS -->
    <script>
        document.addEventListener('scroll', function(){
            const scrolled = window.scrollY;
            const bg = document.querySelector('.hero-bg');
            if(bg) bg.style.transform = 'translateY(' + scrolled * 0.3 + 'px) scale(1.1)';
        });
    </script>

{% endblock %}
