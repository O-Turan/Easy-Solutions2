{% extends 'base.html.twig' %}

{% block title %}
    SIP Trunk - Easy Solutions
{% endblock %}

{% block meta_description %}
    Betrouwbare en flexibele SIP Trunk oplossingen van Easy Solutions. Verbeter uw zakelijke telefonie met schaalbare, kostenefficiënte en hoogwaardige SIP-trunking.
{% endblock %}

{% block body %}

    <!-- HERO SECTION -->
    <section class="hero-section d-flex align-items-center justify-content-center text-center text-white">
        <div class="hero-bg"></div>
        <div class="hero-overlay"></div>
        <div class="container position-relative hero-content">
            <h1 class="display-3 fw-bold animate__animated animate__fadeInDown">SIP Trunk</h1>
            <p class="lead mb-4 animate__animated animate__fadeInUp animate__delay-1s">
                Betrouwbare en flexibele SIP-trunking voor uw zakelijke telefonie.
            </p>
            <a href="#info-section" class="scroll-down">↓</a>
        </div>
    </section>

    <!-- UITLEG SIP TRUNK -->
    <section class="py-5 bg-white" id="info-section">
        <div class="container">
            <h2 class="fw-bold mb-4 text-center">Wat is een SIP Trunk?</h2>
            <p class="text-center mx-auto" style="max-width:750px;">
                Een SIP Trunk is een digitale verbinding die jouw softwareprogramma's en telefooncentrale (PBX) verbindt met een telefoonnetwerk via internet. Dit vervangt traditionele telefoonlijnen en biedt schaalbare, kostenefficiënte en flexibele telefonie. Met SIP Trunking kan je eenvoudig extra lijnen toevoegen, hoge gesprekskwaliteit garanderen en uw bereikbaarheid uitbreiden.
            </p>
        </div>
    </section>

    <!-- INFO CARDS -->
    <section class="py-5 bg-light">
        <div class="container">
            <h2 class="fw-bold text-center mb-4">Waarom kiezen voor SIP Trunk?</h2>
            <p class="text-center mb-5 mx-auto" style="max-width:750px;">
                Onze SIP Trunks zorgen voor stabiele en hoogwaardige telefonie, met flexibiliteit voor uw bedrijf.
            </p>

            <div class="row g-4">
                {% set cards = [
                    {'icon':'📈','title':'Schaalbaar','text':'Voeg eenvoudig nieuwe lijnen toe naarmate jouw bedrijf groeit, zonder extra hardware.'},
                    {'icon':'🔊','title':'Hoge Gesprekskwaliteit','text':'Profiteer van betrouwbare, kristalheldere gesprekken dankzij moderne internetverbindingen.'},
                    {'icon':'💰','title':'Kostenefficiënt','text':'Betaal alleen voor wat je gebruikt en voorkom dure telefoonlijnen.'},
                    {'icon':'⚙️','title':'Flexibel','text':'Integreer eenvoudig met bestaande PBX-systemen en bedrijfsapplicaties voor optimale workflow.'}
                ] %}
                {% for card in cards %}
                    <div class="col-md-6 col-lg-3">
                        <div class="fancy-card h-100 p-4 animate__animated animate__fadeInUp" style="animation-delay: {{ loop.index * 0.2 }}s;">
                            <div class="fancy-icon">{{ card.icon }}</div>
                            <h5 class="mt-3">{{ card.title }}</h5>
                            <p>{{ card.text }}</p>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>

    <!-- CTA SECTION -->
    <section class="py-5 text-center text-white cta-section">
        <div class="container">
            <h2 class="fw-bold mb-3">Klaar voor moderne telefonie met SIP Trunk?</h2>
            <p class="lead mb-4">Neem contact op en ontdek hoe SIP Trunk uw bedrijf efficiënt en bereikbaar maakt.</p>
            <a href="/contact" class="btn btn-cta btn-lg">Contact opnemen</a>
        </div>
    </section>

    <!-- STYLES -->
    <style>
        /* HERO */
        .hero-section{
            height: 70vh;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            overflow: hidden;
        }
        .hero-bg{
            position: absolute;
            inset: 0;
            background: url('{{ asset("assets/sip-trunk-hero.webp") }}') center/cover no-repeat;
            filter: brightness(0.5);
            transform: scale(1.1);
            z-index: 1;
            transition: transform 2s ease;
        }
        .hero-section:hover .hero-bg{
            transform: scale(1.15);
        }
        .hero-overlay{
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(0,31,77,0.6), rgba(179,0,0,0.5));
            z-index: 2;
            animation: overlayPulse 8s infinite alternate;
        }
        @keyframes overlayPulse{
            0%{background: linear-gradient(135deg, rgba(0,31,77,0.6), rgba(179,0,0,0.5));}
            100%{background: linear-gradient(135deg, rgba(0,31,77,0.7), rgba(179,0,0,0.6));}
        }
        .hero-content{position: relative; z-index:3;}
        .scroll-down{
            display: inline-block;
            margin-top: 1rem;
            font-size: 2rem;
            animation: bounce 2s infinite;
            color: #fff;
        }
        @keyframes bounce{
            0%,20%,50%,80%,100%{transform:translateY(0);}
            40%{transform:translateY(-10px);}
            60%{transform:translateY(-5px);}
        }

        /* FANCY CARDS */
        .fancy-card{
            border-radius: 20px;
            background: linear-gradient(135deg, #ffffff, #f0f0f0);
            box-shadow: 0 10px 25px rgba(0,0,0,0.08);
            transition: transform 0.45s, box-shadow 0.45s;
            text-align: left;
            padding: 2rem 1.5rem;
            position: relative;
        }
        .fancy-card:hover{
            transform: translateY(-12px) rotate(-1deg);
            box-shadow: 0 25px 50px rgba(0,0,0,0.15);
        }
        .fancy-icon{
            font-size: 2.2rem;
            background: linear-gradient(135deg, #001f4d, #b30000);
            color: #fff;
            width: 60px;
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            margin-bottom: 1rem;
            transition: transform 0.35s, box-shadow 0.35s;
        }
        .fancy-card:hover .fancy-icon{
            transform: scale(1.3) rotate(15deg);
            box-shadow: 0 10px 25px rgba(0,0,0,0.25);
        }
        .fancy-card h5{
            font-weight: 600;
            font-size: 1.05rem;
            margin-bottom: 0.5rem;
        }
        .fancy-card p{
            font-weight: 400;
            font-size: 0.9rem;
            line-height: 1.6;
            color: #333;
        }

        /* CTA */
        .cta-section{
            background: linear-gradient(135deg,#001f4d,#b30000);
        }
        .btn-cta{
            background: linear-gradient(135deg,#e53935,#b71c1c);
            color: #fff;
            border: none;
            padding: 0.85rem 2.2rem;
            font-weight: 600;
            border-radius: 12px;
            transition: all 0.35s;
        }
        .btn-cta:hover{
            background: linear-gradient(135deg,#b71c1c,#e53935);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 18px 35px rgba(183,28,28,.6);
        }

        /* PARALLAX EFFECT */
        @media (prefers-reduced-motion: no-preference) {
            .hero-bg{will-change: transform;}
        }

        /* RESPONSIVE */
        @media (max-width:768px){
            .hero-section h1{font-size:2.5rem;}
            .hero-section p{font-size:1.1rem;}
            .fancy-card p{font-size:0.85rem;}
            .fancy-card h5{font-size:1rem;}
        }
    </style>

    <!-- Parallax JS -->
    <script>
        document.addEventListener('scroll', function(){
            const scrolled = window.scrollY;
            const bg = document.querySelector('.hero-bg');
            if(bg) bg.style.transform = 'translateY(' + scrolled * 0.25 + 'px) scale(1.1)';
        });
    </script>

{% endblock %}
