{% extends 'base.html.twig' %}

{% block title %}
    White-Label Oplossingen | Easy Solutions
{% endblock %}

{% block meta_description %}
    White-Label Oplossingen van Easy Solutions: lever telecom- en VoIP-diensten onder uw eigen merknaam, volledig beheerd en schaalbaar.
{% endblock %}

{% block body %}

    <!-- HERO SECTION -->
    <section class="hero-section text-white">
        <div class="hero-bg"></div>
        <div class="hero-overlay"></div>

        <div class="hero-inner">
            <h1 class="display-3 fw-bold hero-title">
                White-Label Oplossingen
            </h1>
            <p class="lead hero-subtitle">
                Lever professionele telecomdiensten onder uw eigen merknaam, zonder technische zorgen.
            </p>
        </div>
    </section>

    <!-- UITLEG WHITE-LABEL -->
    <section class="py-5 bg-white">
        <div class="container">
            <h2 class="fw-bold mb-4 text-center">Wat zijn White-Label Oplossingen?</h2>
            <p class="text-center mx-auto description-text">
                Met onze White-Label Oplossingen zijn bedrijven en resellers in staat om complete VoIP en telecomdiensten aan te bieden onder hun eigen naam. Het systeem wordt volledig beheerd door ons, maar uw klanten zien alleen uw merk. Dit betekent dat jij diensten met hoge kwaliteit kunt leveren zonder teveel investeringen te doen om een hele nieuwe systeem te bouwen, terwijl jouw merk centraal staat. Zo kun je concentreren op klantenservice en groei van je bedrijf zonder problemen.
            </p>
        </div>
    </section>

    <!-- INFORMATIE SECTION -->
    <section class="py-5 bg-light">
        <div class="container">
            <h2 class="fw-bold text-center mb-4">Waarom kiezen voor White-Label?</h2>
            <p class="text-center mx-auto mb-5 description-text">
                Met White-Label Oplossingen levert u professionele telecomdiensten onder uw eigen merknaam, volledig beheerd door ons. Flexibel, schaalbaar en zonder technische zorgen.
            </p>

            <div class="row g-4">

                <div class="col-md-6 col-lg-3">
                    <article class="feature-card h-100">
                        <div class="feature-icon">🏷️</div>
                        <h3>Eigen Branding</h3>
                        <p>Uw klanten zien alleen uw merk, logo en communicatie, terwijl wij de technische kant beheren.</p>
                    </article>
                </div>

                <div class="col-md-6 col-lg-3">
                    <article class="feature-card h-100">
                        <div class="feature-icon">⚙️</div>
                        <h3>Volledig Beheerd</h3>
                        <p>Geen zorgen over updates, wij zorgen ervoor dat alles up-to-date blijft.</p>
                    </article>
                </div>

                <div class="col-md-6 col-lg-3">
                    <article class="feature-card h-100">
                        <div class="feature-icon">📈</div>
                        <h3>Schaalbaar</h3>
                        <p>Breid uw diensten eenvoudig uit naarmate uw klanten groeien, zonder extra moeite.</p>
                    </article>
                </div>

                <div class="col-md-6 col-lg-3">
                    <article class="feature-card h-100">
                        <div class="feature-icon">🎯</div>
                        <h3>Focus op Klanten</h3>
                        <p>Richt je volledig op klantenservice en marketing, terwijl wij bezig zijn met de backend development.</p>
                    </article>
                </div>

            </div>
        </div>
    </section>

    <!-- CTA SECTION -->
    <section class="cta-section py-5 text-center text-white">
        <div class="container">
            <h2 class="fw-bold mb-3">Wilt u White-Label diensten aanbieden?</h2>
            <p class="lead mb-4">
                Neem contact met ons op en ontdek hoe u professioneel telecomdiensten onder uw eigen merk kunt leveren.
            </p>
            <a href="{{ path('app_contact') }}" class="btn btn-cta btn-lg">
                Contact opnemen
            </a>
        </div>
    </section>

    <style>

        /* ================= HERO ================= */
        .hero-section{
            position: relative;
            height: 65vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .hero-bg{
            position: absolute;
            inset: 0;
            background: url('{{ asset("assets/white-label-hero.webp") }}') center/cover no-repeat;
            filter: brightness(0.6);
            z-index: 1;
            animation: heroZoom 20s ease-in-out infinite alternate;
        }

        .hero-overlay{
            position: absolute;
            inset: 0;
            background: linear-gradient(
                    135deg,
                    rgba(0,31,77,0.75),
                    rgba(179,0,0,0.55)
            );
            z-index: 2;
        }

        .hero-inner{
            position: relative;
            z-index: 3;
            text-align: center;
            max-width: 900px;
            padding: 0 1rem;
        }

        /* HERO ANIMATIE */
        .hero-title,
        .hero-subtitle{
            opacity: 0;
            transform: translateY(40px);
            animation: fadeUp 1s ease forwards;
        }

        .hero-subtitle{
            animation-delay: .25s;
        }

        /* ================= CONTENT ================= */
        .description-text{
            max-width: 780px;
        }

        /* ================= FEATURE CARDS ================= */
        .feature-card{
            background: linear-gradient(145deg, #fff, #f5f5f5);
            border-radius: 20px;
            padding: 2rem 1.5rem;
            box-shadow: 0 10px 25px rgba(0,0,0,0.08);
            transition: transform .4s ease, box-shadow .4s ease, background .3s ease;
            position: relative;
            text-align: center;
        }

        .feature-card::before{
            content:'';
            position:absolute;
            left:0;
            top:0;
            width:6px;
            height:100%;
            background: linear-gradient(180deg,#001f4d,#b30000);
            border-radius:6px 0 0 6px;
        }

        .feature-card:hover{
            transform: translateY(-12px);
            box-shadow: 0 25px 50px rgba(0,0,0,0.2);
            background: linear-gradient(145deg, #fff, #eaeaea);
        }

        .feature-icon{
            width: 70px;
            height: 70px;
            border-radius: 50%;
            background: linear-gradient(135deg,#001f4d,#b30000);
            color:#fff;
            font-size:2rem;
            display:flex;
            align-items:center;
            justify-content:center;
            margin:0 auto 1rem auto;
            transition: transform .3s ease;
        }

        .feature-card:hover .feature-icon{
            transform: scale(1.3) rotate(10deg);
        }

        .feature-card h3{
            font-weight: 700;
            font-size: 1rem;
            margin-top: 0.75rem;
        }

        .feature-card p{
            font-weight: 400;
            font-size: 0.875rem;
            color: #333;
            line-height: 1.5;
        }

        /* ================= CTA ================= */
        .cta-section{
            background: linear-gradient(135deg,#001f4d,#b30000);
        }

        .btn-cta{
            background:#fff;
            color:#001f4d;
            font-weight:700;
            border-radius:50px;
            padding:.9rem 2.6rem;
            transition:all .3s ease;
        }

        .btn-cta:hover{
            transform: translateY(-3px) scale(1.05);
            box-shadow:0 15px 30px rgba(0,0,0,.25);
        }

        /* ================= ANIMATIONS ================= */
        @keyframes fadeUp{
            to{
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes heroZoom{
            from{ transform: scale(1); }
            to{ transform: scale(1.1); }
        }

        /* ================= RESPONSIVE ================= */
        @media (max-width:768px){
            .hero-section{
                height: 60vh;
            }
            .feature-card h3{
                font-size: 0.95rem;
            }
            .feature-card p{
                font-size: 0.825rem;
            }
        }

    </style>

{% endblock %}
